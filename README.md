# FlashPCA2_AncestryInference
Perform Quality Control (QC), merging and LD-pruning of your dataset(s) before running a PCA using FlashPCA2 and an optionnal projection and ancestry inference using KNN method.
